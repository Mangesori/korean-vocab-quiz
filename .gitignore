# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# MCP config
.mcp.json

# gstack (내부 .git 서브 레포지토리 제외)
.agents/skills/gstack/.git
.gstack/

# fable 오케스트레이션 도구/게이트 (머신별 로컬 도구)
.claude/fable/

# Claude Code 로컬 설정/에이전트 (머신별)
.claude/settings.json
.claude/agents/

# Supabase 로컬 임시
supabase/.temp/

# 환경 변수 — 전부 로컬 전용. 새 변수는 .env.example 에 이름만 추가할 것.
.env
.env.local

# 도움말 스크린샷 캡처 (로컬 전용 — 로컬 Supabase 접속 정보 + 로그인 세션)
.env.capture
scripts/help-shots/.auth/

# 디버그/임시 스크립트 (커밋 대상 아님)
_debug*.mjs
_verify-*.mjs
debug-*.png
patch/

# 채점 모델 평가 산출물 (학생 문장 포함 — 커밋 금지)
scripts/eval-grading/out/*
!scripts/eval-grading/out/report-demo.html
